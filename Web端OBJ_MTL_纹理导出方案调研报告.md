# Web 端 OBJ + MTL + 纹理导出方案调研报告

## 调研结论
目前**不存在**一个现成的、开箱即用的单一库，能够完美地在 Web 环境（特别是 Three.js）中一次性导出包含 OBJ 几何体、MTL 材质定义以及所有关联纹理图片的完整包。

### 现有工具现状
1.  **THREE.OBJExporter**:
    -   **优点**: Three.js 官方内置，处理几何体非常稳定。
    -   **缺点**: **不支持**导出 `.mtl` 文件。**不支持**导出纹理文件。它仅输出 `.obj` 文本内容。

2.  **THREE.MTLExporter**:
    -   Three.js 旧版示例中曾存在 `MTLExporter`，但目前大多已不再维护或被 GLTF 取代。它通常需要手动干预才能与 OBJ 正确匹配。

3.  **GLTF (推荐替代方案)**:
    -   Web 3D 导出的行业标准是 **GLTF/GLB**。它原生支持在单个文件中嵌入几何体、材质和纹理。
    -   但是，如果项目强制要求使用 OBJ 格式，则无法使用 GLTF。

## 推荐方案：自定义实现
为了实现完整的导出（OBJ + MTL + 纹理），我们需要构建一个包含以下步骤的自定义工作流：

### 1. 生成几何体 (.obj)
使用 `THREE.OBJExporter` 生成几何体数据字符串。

### 2. 生成材质 (.mtl)
编写自定义函数遍历 Three.js 的材质并生成 `.mtl` 字符串。
-   将 `MeshStandardMaterial` / `MeshPhongMaterial` 的属性映射到 MTL 标签（如 `Kd`, `Ks`, `Ns`）。
-   识别纹理贴图（`map`, `normalMap` 等）并将文件名写入 MTL（如 `map_Kd`, `map_Bump`）。

### 3. 提取纹理 (图片)
将场景中使用的纹理转换为可下载的文件（Blob）。
-   将纹理图像绘制到 HTML `<canvas>` 上。
-   将 canvas 转换为 Blob 数据（PNG/JPG 格式）。

### 4. 打包 (ZIP)
使用 **JSZip** 将所有资源打包成一个压缩包供下载。
-   `model.obj`
-   `model.mtl`
-   `textures/texture1.png`
-   `textures/texture2.png`

## 局限性与风险 (用于汇报)
虽然此自定义方案可以实现导出功能，但受限于 OBJ 格式的年代久远以及 Web 环境的限制，存在固有的局限性。

### 1. 格式局限性 (OBJ/MTL 已过时)
*   **不支持 PBR**: OBJ/MTL 格式早于现代的基于物理的渲染 (PBR)。它使用的是较旧的 Blinn-Phong 光照模型。
    *   *影响*: Three.js `MeshStandardMaterial` 的 **金属度 (Metalness)** 和 **粗糙度 (Roughness)** 等属性无法在标准 MTL 文件中原生表示。导出的模型在其他软件中看起来会不一样（通常会有“塑料感”）。
    *   *变通*: 部分软件支持非标准的 MTL 扩展（如 `map_Pr`, `map_Pm`），但兼容性参差不齐。
*   **仅限静态几何体**: OBJ **不支持**动画、蒙皮或骨骼绑定。它仅适用于静态网格。
*   **层级结构简单**: 虽然支持组，但缺乏 GLTF 那样复杂的场景图能力。

### 2. 技术与性能限制
*   **浏览器内存限制**: 在浏览器端（客户端）生成包含高分辨率纹理的 ZIP 文件会消耗大量内存。
    *   *风险*: 导出非常大的模型或 4K/8K 纹理可能会导致内存受限设备（尤其是移动设备）的浏览器标签页崩溃。
*   **纹理格式**: 我们必须将纹理转换为标准格式（PNG/JPG）。如果源文件使用的是压缩的 GPU 纹理（KTX/DDS），则必须先进行解码，这在计算上非常昂贵且缓慢。

### 3. 实现复杂度
*   **维护成本**: 由于没有针对此特定“Three.js 转 OBJ+MTL+Zip”工作流的标准库，我们需要编写自定义代码。如果 Three.js 更改其内部材质结构，代码将需要维护。
*   **保真度丢失**: 由于格式不匹配（PBR -> Blinn-Phong），导出的视觉效果**永远无法**与 Web 视图 1:1 匹配。

### 4. 最终建议
如果目标是**完美的视觉保真度**或**在现代工具中编辑**（Blender, Maya, Unity），**GLTF/GLB** 是绝对优越的选择。
仅在下游需求明确要求必须支持旧版 OBJ 时，才使用此 OBJ+MTL 方案。
